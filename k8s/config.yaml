apiVersion: v1
kind: ConfigMap
metadata:
  name: spring-cloud
  namespace: default
data:
  application.yml: |-
    surname: white
    name: walter
    spring:
      profiles:
        actives: prod
      application:
        name: foo