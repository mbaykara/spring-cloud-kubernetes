spring:
  cloud:
    kubernetes:
      enabled: true
      reload:
        enabled: true
        mode: event

management:
  endpoint:
    restart:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "refresh"